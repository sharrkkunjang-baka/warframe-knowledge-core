'use strict';const{pinyin}=require('pinyin-pro');const norm=v=>String(v||'').normalize('NFKC').trim().toLowerCase().replace(/[\s·・‧•_-]+/g,'');function createResolver(a){const c=[];for(const[k,v]of Object.entries(a.frames||{}))for(const x of[k,...v])c.push({alias:x,canonical:k});for(const[x,k]of Object.entries(a.terms||{}))c.push({alias:x,canonical:k});return q=>{const exact=c.find(x=>norm(x.alias)===norm(q));if(exact)return{...exact,match:'exact',score:200};const qt=pinyin(q,{toneType:'none',type:'array'});const ranked=c.map(x=>{const ct=pinyin(x.alias,{toneType:'none',type:'array'});let m=0,j=0;for(const t of qt){while(j<ct.length&&ct[j]!==t)j++;if(j<ct.length){m++;j++;}}return{...x,score:m*30-(qt.length-m)*16-(ct.length-m)*4};}).sort((x,y)=>y.score-x.score);return ranked[0]?.score>=50?{...ranked[0],match:'pinyin-weighted'}:null;};}module.exports={createResolver,normalize:norm};
