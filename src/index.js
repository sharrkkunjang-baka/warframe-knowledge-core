'use strict';const path=require('path'),{loadData}=require('./loader'),{createResolver,normalize}=require('./resolver');function search(q,e){q=normalize(q);return e.filter(x=>[x.title,...x.aliases].some(v=>normalize(v).includes(q))||normalize(x.content).includes(q));}function createKnowledgeCore(o={}){const data=loadData(o.root||path.join(__dirname,'..'),o),resolveName=createResolver(data.aliases);return{...data,resolveName,normalizeTerms:t=>Object.entries(data.aliases.normalization||{}).reduce((s,[a,b])=>s.split(a).join(b),String(t||'')),searchFacts:q=>search(q,data.facts),searchKnowledge:q=>search(q,data.knowledge),buildWikiContext:q=>{const facts=search(q,data.facts),knowledge=search(q,data.knowledge),resolution=resolveName(q);if(!facts.length&&!knowledge.length&&!resolution)return null;return{query:q,resolution,facts,knowledge,text:[...facts,...knowledge].map(x=>`【${x.title}】\n${x.content}`).join('\n\n')};}};}module.exports={createKnowledgeCore};
