# 上线审核

审核者需要逐项确认：

1. 内容与来源一致，没有把推测写成事实。
2. 官方译名与当前简中客户端一致；社区叫法已明确标注。
3. `gameVersion` 和 `updatedAt` 合理。
4. 别名不会明显污染短词查询。
5. 攻略、评级和价格策略明确说明评价目标与主观性。
6. 不包含凭据、个人信息和部署路径。

确认后将 `reviewStatus` 设为 `approved` 并在 `reviewedBy` 中写入审核者 GitHub ID。构建器只发布已审核条目。
